CREATE TABLE nazwiska(
nr_ucznia INTEGER,
nazwisko VARCHAR(30),
imie1 VARCHAR(30),
imie2 VARCHAR(30)
);

CREATE TABLE dane(
dzien INTEGER,
miesiac INTEGER,
rok INTEGER,
miasto_urodzenia TEXT,
wojewodztwo TEXT
);

CREATE TABLE oceny(
zachowanie VARCHAR(20),
rel_et INTEGER,
jpol INTEGER,
jand INTEGER,
jniem INTEGER,
mat INTEGER,
hist INTEGER,
geog INTEGER,
biol INTEGER,
fiz INTEGER,
che INTEGER,
tech INTEGER,
info INTEGER,
plas INTEGER,
po INTEGER,
wf INTEGER
);












       / | \
       \   /
       |   |
       |   |
       |   |
       |   |
     / \   / \
    |   | |   |
     \ /   \ /
